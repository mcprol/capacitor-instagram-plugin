/*
import { WebPlugin } from '@capacitor/core';
import { InstagramPluginProtocol } from './definitions';

export class InstagramWeb extends WebPlugin implements InstagramPluginProtocol {
  constructor() {
    super({
      name: 'Instagram',
      platforms: ['web'],
    });
  }
  async shareLocalMedia(_medias: string, _mediaType: string, _target: string): Promise<{ results: any[]; }> {
    throw new Error('Method not implemented.');
  }
}

const Instagram = new InstagramWeb();

export { Instagram };

import { registerWebPlugin } from '@capacitor/core';
registerWebPlugin(Instagram);
*/
